import { Component }          from '@angular/core';

@Component({
    //moduleId: module.id,
    selector: 'my-app',
    template: `
    <h1>{{title}}</h1>
    
    `
})
export class AppComponent {
    title = 'Roboine-Shop';
}